/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'avatar.vercel.sh', 'media.licdn.com', 'medcuj', 'i.pinimg.com'],
    },
  };
  
export default nextConfig;
